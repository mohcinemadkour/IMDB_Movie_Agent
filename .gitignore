# Environment & secrets — NEVER commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ChromaDB / Pinecone artefacts — not committed (too large / managed externally)
# Streamlit runtime cache (but commit config.toml for deployment)
.streamlit/credentials.toml

# Application logs
logs/

# LLM response cache
.cache/

# ChromaDB local persistent index
.chroma/
