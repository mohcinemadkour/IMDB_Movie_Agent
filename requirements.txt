# Direct dependencies — pinned to exact versions for reproducibility.
# For a full transitive lock, see requirements.lock.

# Core
streamlit==1.56.0
pandas==3.0.2
python-dotenv==1.2.2

# LLM providers
openai==2.30.0
google-generativeai==0.8.6

# LangChain agent + tools
langchain==1.2.15
langchain-openai==1.1.12
langchain-google-genai==4.2.1
langchain-community==0.4.1
langgraph==1.1.6

# Vector store — FAISS is the default local backend (committed to repo).
# To use Pinecone or ChromaDB instead, uncomment the relevant lines and
# set VECTOR_STORE=pinecone or VECTOR_STORE=chroma in your .env.
faiss-cpu==1.13.2
# pinecone==5.4.2          # required for VECTOR_STORE=pinecone
# langchain-pinecone==0.2.0
# chromadb==0.6.3           # required for VECTOR_STORE=chroma
# langchain-chroma==0.2.4

# Voice I/O
gTTS==2.5.4
audio-recorder-streamlit==0.0.10

# Observability
python-json-logger==4.1.0

# Testing & dev tooling
pytest==9.0.3
pytest-mock==3.15.1
ruff==0.15.9
mypy==1.20.0
pandas-stubs==3.0.0.260204
